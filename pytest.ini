[pytest]
pythonpath = .
testpaths = tests
markers =
    be: backend tests
    fe: frontend tests
    events: events page / events API tests
    admin: admin page tests
    orders: orders page tests
    sanity: high-value smoke tests
    live_api: tests that hit the live API
    component: backend component tests
